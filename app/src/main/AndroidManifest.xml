<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.fazziclay.schoolguide">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestNetworkData">
        <activity
            android:name=".android.activity.RestorePointActivity"
            android:exported="true" />
        <activity
            android:name=".android.activity.ScheduleConfiguratorActivity"
            android:exported="true" />
        <activity
            android:name=".android.activity.SettingsActivity"
            android:exported="true" />

        <receiver
            android:name=".android.widgets.MainWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/main_widget_info" />
        </receiver>

        <activity
            android:name=".android.activity.HomeActivity"
            android:exported="true" />

        <service
            android:name=".android.service.ForegroundService"
            android:enabled="true"
            android:exported="false" />
        <service
            android:name=".android.service.MainNotificationService"
            android:enabled="true"
            android:exported="false" />

        <activity
            android:name=".android.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>